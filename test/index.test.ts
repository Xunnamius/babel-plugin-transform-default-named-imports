import pluginTester from 'babel-plugin-tester'
import plugin from '../src/index'

describe('babel-plugin-transform-mjs-imports', () => {
    it('(todo)', async () => {
        expect.hasAssertions();

        void plugin;
    });
});
